--1
--select d.Dependent_name , d.Sex from dbo.Dependent d , dbo.Employee e where e.SSN=d.ESSN and d.Sex like 'f' and e.Sex like 'f'
--union all
--select d.Dependent_name , d.Sex from dbo.Dependent d , dbo.Employee e where e.SSN=d.ESSN and d.Sex like 'm' and e.Sex like 'm'
--2
--select Pname , sum (hours) from dbo.Project , dbo.Works_for group by Pname
--3
--select dname ,max(salary),min(salary),avg(salary) from dbo.Departments,dbo.Employee group by Dname
--4
--select fname, lname from Employee, Departments where ssn = mgrssn and mgrssn not in (select essn from Dependent)
--5
--select dnum, dname, count(ssn) 'Number of employees in dept' from departments d left outer join employee e on dnum = dno group by dnum, dname having avg(isnull(salary,0)) < (select avg(salary) from employee)
--6
--select * from Employee where salary in (select distinct top(2) salary  from Employee order by salary desc)
--7
--insert into dbo.Employee (dno,ssn,superssn, salary) values (30,102672,112233,3000)
--8
--update dbo.Employee set salary=salary*1.2 where ssn=102672
--9
--insert into dbo.departments (dname,dnum,MGRSSN,[MGRStart Date]) values ('DEPT IT' ,100,112233,'1-11-2006')
--10
--create table Items (Itm_No int primary key , Itm_Name varchar(50) , Itm_Price int)
--11
--create table Orders (Order_No int Primary key, Order_Date date)
--12
--create Table Order_Items (Itm_No int, Order_no int, constraint OI_PK Primary Key (Itm_No,Order_no))
--13
--alter table Items add constraint pr_def default '10' for itm_price
--14
--alter table orders alter column order_date date not null
--15
--create Table Emp_Order (ESSN int, Order_no int, constraint EO_PK Primary Key (ESSN, Order_no))
--16
--alter table orders add constraint Or_Un unique (order_no)
